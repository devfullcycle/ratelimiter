services:
  app:
    build: .
    working_dir: /app
    ports:
      - "8080:8080"
      - "9090:9090" # For future gRPC support
    volumes:
      - .:/app
    environment:
      - GO_ENV=test
      - CGO_ENABLED=1
    # No bind mount in CI to ensure clean environment
